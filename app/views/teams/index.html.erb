<ul>
<% teams.each do |team| %>
<li>  <%= link_to(team.name + " (#{team.players.count})", team_path(team.id), class: "team-link") %></li>
<% end %>
</ul>

<div> <%=  link_to("Create New Team", new_team_path, class: "button") %> </div>
